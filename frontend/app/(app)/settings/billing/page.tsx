import { BillingSettings } from "@/components/app/settings/billing-settings";

export const metadata = { title: "Billing" };

export default function BillingSettingsPage() {
  return <BillingSettings />;
}
